# ============================================================================
# n8n Local Development Environment Configuration
# ============================================================================
# Copy this file to .env and customize as needed for local development
#
# This configuration is optimized for local development with:
# - Direct port mapping (no reverse proxy)
# - Relaxed security settings
# - Detailed logging for debugging
# ============================================================================

# ============================================================================
# BASIC CONFIGURATION
# ============================================================================
# Local development settings
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
TIMEZONE=UTC

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL settings for local development
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n_dev_password
POSTGRES_DB=n8n_dev
POSTGRES_PORT=5432

# ============================================================================
# REDIS CONFIGURATION
# ============================================================================
# Redis settings (no password required for local dev)
REDIS_PORT=6379

# ============================================================================
# AUTHENTICATION (Optional for local development)
# ============================================================================
# Basic auth - disabled by default for easier local development
N8N_BASIC_AUTH_ACTIVE=false
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=admin

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================
# Logging level (debug for development, info for testing)
N8N_LOG_LEVEL=info

# Development features
N8N_DIAGNOSTICS_ENABLED=true
N8N_PERSONALIZATION_ENABLED=true
N8N_VERSION_NOTIFICATIONS_ENABLED=true

# Performance settings
N8N_PAYLOAD_MAX_SIZE=16

# ============================================================================
# DEVELOPMENT NOTES
# ============================================================================
#
# 1. ACCESS YOUR n8n INSTANCE:
#    - URL: http://localhost:5678
#    - No authentication required by default
#
# 2. DATABASE ACCESS:
#    - Host: localhost
#    - Port: 5432
#    - Database: n8n_dev
#    - User/Password: as configured above
#
# 3. REDIS ACCESS:
#    - Host: localhost
#    - Port: 6379
#    - No password required
#
# 4. USEFUL COMMANDS:
#    - n8n-ready up      # Start all services
#    - n8n-ready down    # Stop all services
#    - n8n-ready doctor  # Check system requirements
#
# 5. DEBUGGING:
#    - View logs: docker-compose logs -f n8n
#    - Check status: docker-compose ps
#    - Restart service: docker-compose restart n8n
#
# 6. DATA PERSISTENCE:
#    - All data is stored in Docker volumes
#    - Data persists between container restarts
#    - To reset: docker-compose down -v (WARNING: deletes all data!)
#
# ============================================================================